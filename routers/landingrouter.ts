// import express from "express";
// import { User } from "../types"; // Zorg ervoor dat User goed is geïmporteerd

// export function landingRouter() {
//     const router = express.Router();

//     // Landingspagina route
//     router.get("/", (req, res) => {
//         const loggedIn = req.session.user ? true : false; // Controleer of de gebruiker is ingelogd
//         res.render("landing", { loggedIn });
//     });

//     return router;
// }
